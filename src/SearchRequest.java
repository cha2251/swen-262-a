public class SearchRequest implements Request {
    @Override
    public String execute(String[] args) {
        if(args.length >= 3) {

        }
        return "info,missing-parameters,{id};";
    }
}
